@model IEnumerable<Agricathon_gr3.Models.TypeResult>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TypeR)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TypeR)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TypeRId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TypeRId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TypeRId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
